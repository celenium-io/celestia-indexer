// SPDX-FileCopyrightText: 2025 Bb Strategy Pte. Ltd. <celenium@baking-bad.org>
// SPDX-License-Identifier: MIT

// Code generated by go-enum DO NOT EDIT.
// Version: v0.9.2

// Built By: go install

package types

import (
	"database/sql/driver"
	"fmt"
	"strings"

	"github.com/pkg/errors"
)

const (
	// BlobSourcePfb is a BlobSource of type pfb.
	BlobSourcePfb BlobSource = "pfb"
	// BlobSourceFibre is a BlobSource of type fibre.
	BlobSourceFibre BlobSource = "fibre"
)

var ErrInvalidBlobSource = fmt.Errorf("not a valid BlobSource, try [%s]", strings.Join(_BlobSourceNames, ", "))

var _BlobSourceNames = []string{
	string(BlobSourcePfb),
	string(BlobSourceFibre),
}

// BlobSourceNames returns a list of possible string values of BlobSource.
func BlobSourceNames() []string {
	tmp := make([]string, len(_BlobSourceNames))
	copy(tmp, _BlobSourceNames)
	return tmp
}

// BlobSourceValues returns a list of the values for BlobSource
func BlobSourceValues() []BlobSource {
	return []BlobSource{
		BlobSourcePfb,
		BlobSourceFibre,
	}
}

// String implements the Stringer interface.
func (x BlobSource) String() string {
	return string(x)
}

// IsValid provides a quick way to determine if the typed value is
// part of the allowed enumerated values
func (x BlobSource) IsValid() bool {
	_, err := ParseBlobSource(string(x))
	return err == nil
}

var _BlobSourceValue = map[string]BlobSource{
	"pfb":   BlobSourcePfb,
	"fibre": BlobSourceFibre,
}

// ParseBlobSource attempts to convert a string to a BlobSource.
func ParseBlobSource(name string) (BlobSource, error) {
	if x, ok := _BlobSourceValue[name]; ok {
		return x, nil
	}
	return BlobSource(""), fmt.Errorf("%s is %w", name, ErrInvalidBlobSource)
}

// MarshalText implements the text marshaller method.
func (x BlobSource) MarshalText() ([]byte, error) {
	return []byte(string(x)), nil
}

// UnmarshalText implements the text unmarshaller method.
func (x *BlobSource) UnmarshalText(text []byte) error {
	tmp, err := ParseBlobSource(string(text))
	if err != nil {
		return err
	}
	*x = tmp
	return nil
}

// AppendText appends the textual representation of itself to the end of b
// (allocating a larger slice if necessary) and returns the updated slice.
//
// Implementations must not retain b, nor mutate any bytes within b[:len(b)].
func (x *BlobSource) AppendText(b []byte) ([]byte, error) {
	return append(b, x.String()...), nil
}

var errBlobSourceNilPtr = errors.New("value pointer is nil") // one per type for package clashes

// Scan implements the Scanner interface.
func (x *BlobSource) Scan(value interface{}) (err error) {
	if value == nil {
		*x = BlobSource("")
		return
	}

	// A wider range of scannable types.
	// driver.Value values at the top of the list for expediency
	switch v := value.(type) {
	case string:
		*x, err = ParseBlobSource(v)
	case []byte:
		*x, err = ParseBlobSource(string(v))
	case BlobSource:
		*x = v
	case *BlobSource:
		if v == nil {
			return errBlobSourceNilPtr
		}
		*x = *v
	case *string:
		if v == nil {
			return errBlobSourceNilPtr
		}
		*x, err = ParseBlobSource(*v)
	default:
		return errors.New("invalid type for BlobSource")
	}

	return
}

// Value implements the driver Valuer interface.
func (x BlobSource) Value() (driver.Value, error) {
	return x.String(), nil
}
